# import torch
# print(torch.cuda.is_available())  # Should return True if CUDA is installed and available
# print(torch.cuda.device_count())  # Should return the number of GPUs available

# just for testing, an algorithm that creates all combinations of 1, 0 of length n
# through creating n lists of 2^n elements and then transposing them
